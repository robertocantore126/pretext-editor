// Barrel. FROZEN - add types to types/doc.ts (A) or types/layout.ts (B) instead.

export type { Cursor, Selection, TextMark, SerializedImage, SerializedDocument } from './types/doc'
export type { FloatImage, LineInfo } from './types/layout'
